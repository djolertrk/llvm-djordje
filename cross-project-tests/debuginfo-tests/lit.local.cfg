if "clang" not in config.available_features:
    config.unsupported = True

# SBPF has a standalone suite requiring an explicit interpreter and toolchain.
config.excludes = list(config.excludes) + ["sbpf"]
